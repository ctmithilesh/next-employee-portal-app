import React from 'react'

const RegisterLabel = ({ name }) => {
  return (
    <label className="label">
          <span className="label-text">{name} </span>
    </label>
  );
}

export default RegisterLabel
